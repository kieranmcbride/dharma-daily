import React from 'react';
import ReactDOM from 'react-dom/client';
import { DailyTeachingScreen } from './screens/DailyTeachingScreen';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DailyTeachingScreen />
  </React.StrictMode>
);
